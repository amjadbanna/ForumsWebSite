meta {
  name: Set User Status
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/admin/users/REPLACE_WITH_USER_ID/status
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "status": "blocked"
  }
}
